#include "allocator.h"

namespace ECS
{

    class DynamicArray
    {
    public:
        void Init(Allocator* allocator, uint32_t capacity)
        

    private:
        void* array;
        uint32_t count;
        uint32_t capacity;
    };

}
